April 11

What is assembly?
-A simpler coding program that only requires the code necessary for it to work. 

What is it good for?
-Assembly its good for system that needs total control over it.

How is assembly different than Java?
-Assembly is more complex to learn than from Java.

What are the differences between high level and low level languages?
-High level languages are harder to work with while low level languages are simpler and efficient.

Which type is Java? How do you know, i.e., what characteristics does it have of that language type?
-Java is high level because the program developer is faster, has an easier language, and faster efficiency 

What type is Assembly? How do you know, i.e., what characteristics does it have of that language type?
-It is low level because it states that the proggram its easier to work with than with Java.
